To build:
    ./configure
    make

If you don't have a configure file, run 'sh autogen.sh'

Note - building requires several tools:
    autoconf
    automake
    boost c++ headers
    libtool
    maven
    mpicxx
    mpirun
